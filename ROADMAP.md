# خارطة طريق GT-SIRM

> آخر تحديث: 2026-06-06 — بدء البنية الأساسيّة (v0.1.0)
> هذا الملف مشترك بين نسختَي سطح المكتب والويب.

---

## 🌙 إعلان النيّة

بُنِيَ هذا المشروع ابتغاءَ وجه الله الكريم، خِدمةً للإسلام والمسلمين. كلّ ميزة تُضاف تخدم هذا الغرض، ولا تُقبل ميزة تخالف الشرع أو تفتح باباً لمعصية.

---

## 🎯 المبادئ الموجِّهة

1. **تماثل المزايا بين النسختَين** — كلّ ميزة تُطبَّق في `GT-SIRM-DESKTOP/` و `GT-SIRM-WEB/` في نفس Commit
2. **توارُث ذكيّ** — البناء على ميزات GT-SQRM v3.0 المُختبَرة، لا إعادة كتابة
3. **العمل دون اتصال** — كلّ المحتوى الإسلاميّ متاح offline (قرآن، حديث، أذكار، أدعية)
4. **مرونة الاستيراد** — المستخدم يُحضِر نصّه أو صوته أو فيديوه بأبسط طريقة
5. **توگل الوحدات** — كلّ وحدة محتوى قابلة للإلغاء، لا فرض على المستخدم
6. **التزام شرعيّ** — التحقّق من تصحيح الأحاديث، توثيق مصادر الأقوال

---

## 📦 المراحل

### v0.1.0 — البنية الأساسيّة ✅ (2026-06-06)
- إنشاء هيكل المستودع
- الملفات التعريفيّة المشتركة
- صفحة الموقع الرسميّ
- ملفّات تقنيّة Skeleton

### v0.2.0 — Fork من GT-SQRM v3.0
- نسخ `GT-SQRM-main/` إلى `GT-SIRM-DESKTOP/` و `GT-SQR-main/` إلى `GT-SIRM-WEB/`
- توحيد البادئات: `gt_sqrm_` و `gt_sqr_` → `gt_sirm_` (نفس البادئة للنسختَين لأنّ المستودع موحَّد)
- تحديث أسماء التطبيق والأيقونات
- التحقّق من بناء الحزم الثلاث (AppImage/DEB/RPM)

### v0.3.0 — Module Manager
- إضافة `data-module="..."` لكلّ قسم
- تبويب الإعدادات الجديد بـ توگل لكلّ وحدة
- وحدات: `quran` · `hadith` · `azkar` · `asma` · `duas` · `hikam` · `free`
- حفظ تفضيلات المستخدم في localStorage

### v0.4.0 — محرّر النصّ الحرّ
- استبدال "اختيار آية" بـ محرّر متعدّد الأسطر
- توقيت لكلّ شريحة (يدويّ أو موزَّع تلقائياً)
- استخدام نفس قسم التنسيق والتأثيرات
- قوالب نصّيّة قابلة للحفظ

### v0.5.0 — مرونة الاستيراد
- drag-drop شامل لكلّ المصادر (صوت/فيديو/صورة)
- خيار **تجريد صوت فيديو الخلفية** + ربط صوت منفصل
- استيراد ألوان خلفية (مفرد · تدرّج · شعاعيّ)
- معاينة وتقطيع زمنيّ

### v0.6.0 — أداة Chromakey
- WebGL fragment shader للمعاينة في الوقت الحقيقيّ
- ffmpeg `colorkey`/`chromakey` filter للتصدير (سطح المكتب)
- نفس shader في WebCodecs (الويب)
- UI: color picker + similarity + blend + edge softening

### v0.7.0 — وحدة الحديث الشريف
- متصفّح بشجرة: الكتاب → الباب → الحديث
- مصدر: Sunnah.com API أو حزمة JSON محليّة (يُحسَم وقت التطبيق)
- شارة درجة التصحيح: 🟢 صحيح / 🟡 حسن / 🔴 ضعيف
- بحث في النصّ بـ تطبيع التشكيل (مُورَث من GT-SQRM)

### v0.9.0 — وحدة الأذكار ✅ (2026-06-09)
- استيراد كامل (132 فئة، 267 ذكراً) من مشروع [GT-HISNMUSLIM](https://github.com/SalehGNUTUX/GT_HISNMUSLIM) بإذن المؤلِّف نفسه
- متصفّح: أذكار الصباح / المساء / النوم / الاستيقاظ / بعد الصلاة
- عدّاد التكرار (مثل: 33 مرّة)

### v0.10.0 — أسماء الله الحسنى + الأدعية + الحِكَم ✅ (2026-06-09)
- **أسماء الله الحسنى**: 99+1 اسماً مع المعاني والشواهد القرآنيّة
- **الأدعية المأثورة**: 32 دعاءً مصنّفاً في 5 فئات (من القرآن، الكرب، الصباح/المساء، يوميّة، استخارة/استغفار)
- **الحِكَم**: 32 قولاً للسلف بإسناد لقائله في 4 فئات (الصحابة، التابعون، الأئمّة الأربعة، ابن القيّم وابن تيمية)

### v0.10.0 — مصادر الصوت
- **(أ)** تسجيل ميكروفون عبر `getUserMedia` + `MediaRecorder`
- **(ب)** TTS عربيّ — يُحسَم بين Edge TTS (سحابيّ) و Coqui (محليّ)
- **(ج)** الوضع الصامت مع أصوات خلفيّة فقط (للأناشيد الإيقاعيّة بدون مؤثرات صوتية)

### v1.0.0 — تحزيم ونشر
- بناء حزم سطح المكتب: AppImage + DEB + RPM
- تحضير ZIP لنسخة الويب (استضافة ذاتيّة)
- نشر على GitHub Releases (release واحد يحوي الأربعة)
- نشر نسخة الويب على GitHub Pages
- 8 قوالب موضوعيّة جاهزة (صباح/مساء/حديث/دعاء/حكمة/سيرة/قصة/اسم)

---

## 🐛 v1.1.1 — إصلاح مقاطع الخَلفيّة المُتعدّدة (قيد التَنفيذ)

**التاريخ:** 2026-07-12 — **التَحقيق مُكتَمِل، الإصلاح مُؤجَّل لِلجَلسة القادمة**

### الأَعراض المُبلَّغ عنها من المُستخدم
1. عند تَحميل أَكثر من مَقطع خَلفيّة: تَصدير غير سَلِس بَين المقاطع
2. أَحياناً حتى في المُعاينة لا يَنتقِل بسَلاسة
3. المقاطع تَظهر وكأنّها في وَضع تَسريع
4. المقاطع تَمُرّ واحدة فَوق الأُخرى بشَكل غَريب

### الأَعطاب الأَربعة المُكتَشَفة

#### 🔴 عَطَب #1 — تَصدير الويب/APK يَعبَث بـplaybackRate حتى 16×
**المَوقِع:** `GT-SIRM-WEB/export-engine-web.js:378-391` (`syncBgVidPlayback`)

```js
const ratio = wallClockSec > 0 ? (ourClockSec / wallClockSec) : 1;
const newRate = Math.max(0.25, Math.min(16, ratio));
vid.playbackRate = newRate;  // ← حتى 16× سُرعة!
```
- يُطبَّق فَقط على `S.bgVid` وليس على `S.bgVidNext` (القادم في الـcrossfade)
- خلال الـcrossfade: الحاليّ 16× والقادِم 1× → يَظهران "يَمُرّان فَوق بَعض"
- الـencoder أَسرع من realtime دائماً → دائم التَسريع
- **لا يوجَد في سَطح المكتب** (يَستَخدِم استخراج frames مُسبَق من ffmpeg)

**الإصلاح المُقترَح:** استبدال `syncBgVidPlayback` بـ**deterministic seek** — لكُلّ إطار `t` نَحسب `(clipIndex, localTime)` من `clipDurations + xf` ونَستَدعي `seekVideoToTimeWeb` (نَفس نَمط recvid).

#### 🔴 عَطَب #2 — قُفل التَقليم في المُعاينة (trim deadlock)
**المَوقِع:** `app.js:4878-4895` (كِلا النُسختَين)

```js
S.bgVid._trimHandler = () => {
  if (S.bgVid.currentTime >= tt.end - 0.05) {
    S.bgVid.currentTime = tt.start;  // ← يَعود للبداية دائماً، ended لا يُطلَق
  }
};
```
- عند تَفعيل تَقليم الخَلفيّة مَع playlist: المقطع الأَوّل يَدور بَين trim.start و trim.end إلى الأَبد
- `ended` لا يُطلَق → `switchToNextBgVid` لا يَعمَل → يَبقى المقطع الأَوّل فَقط
- الـarrow يَلتَقِط `S.bgVid` المُتحوّل عالمياً → cross-clip corruption مُحتَمَل

**الإصلاح المُقترَح:** إمّا (أ) تَعطيل الـtrim handler عند `bgVidItems.length >= 2`، أَو (ب) استدعاء `switchToNextBgVid()` بَدَل الرُجوع لـtrim.start. + إصلاح الـarrow لِيَلتَقِط `vid` مُحدَّداً.

#### ⚠️ عَطَب #3 — بَذرة playlist بلا crossfade في تَصدير سَطح المكتب
**المَوقِع:** `GT-SIRM-DESKTOP/src/main/main.js:1107` (`-stream_loop -1`)

- xfade مُطبَّق بَين المقاطع بشَكل صَحيح
- لكن عند إعادة الحَلقة (إن كانت المُدّة الكُلّيّة > مَجموع المقاطع): يَعود للأَوّل بـ**قَطع حادّ**

**الإصلاح المُقترَح:** إمّا (أ) إضافة xfade يَلُفّ من آخر مَقطع لأوَّله، أَو (ب) قَبول القَطع مَع تَوثيق.

#### 🟡 عَطَب #4 — مقاطع مَنسيّة تُشغَّل في الخَلفيّة
**المَوقِع:** `switchToNextBgVid` (كِلا النُسختَين، ~app.js:6540/5758)

- المقطع القَديم لا يُوقَف بَعد التَبديل → تَتراكَم عناصر `HTMLVideoElement` تُشغَّل دون داعٍ

**الإصلاح المُقترَح:** إضافة `oldVid.pause()` في `switchToNextBgVid`.

### حالة التَفكير في التَنفيذ

قَرارات مُعلَّقة للجَلسة القادمة:
1. أَربعة إصلاحات في v1.1.1 أَم فَقط #1 + #4 مُركَّز؟
2. لِـ#2: (أ) تَعطيل trim مَع multi-bg (أَبسَط)، أَم (ب) switch عند نِهاية trim (أَقوى)؟
3. لِـ#3: xfade يَلُفّ أَم تَوثيق فَقط؟
4. رَقم الإصدار: v1.1.1 (patch) أَم v1.2.0 (فَرعيّ)؟

### تَخطيط تَقنيّ للـdeterministic seek (عَطَب #1)

```js
// خُطوط عَريضة لِلدالّة المُقترحة في export-engine-web.js
function getBgClipAtTime(t, clipDurations, xf) {
  let cum = 0;
  for (let i = 0; i < clipDurations.length; i++) {
    const clipEnd = cum + clipDurations[i];
    if (t < clipEnd) {
      const localTime = t - cum;
      const remaining = clipEnd - t;
      const inXfade = (remaining <= xf && i < clipDurations.length - 1);
      return { clipIndex: i, localTime, inXfade, xfadeAlpha: inXfade ? (1 - remaining / xf) : 0 };
    }
    cum += clipDurations[i] - xf; // نَفس رياضيّات ffmpeg xfade
  }
  // Loop back
  const cycleDur = cum + xf; // + last clip's xf overlap accounted for
  return getBgClipAtTime(t % cycleDur, clipDurations, xf);
}
```

ثُمّ في حَلقة الإطارات:
```js
const {clipIndex, localTime, inXfade, xfadeAlpha} = getBgClipAtTime(t, clipDurations, xf);
await seekVideoToTimeWeb(S.bgVidItems[clipIndex].vid, localTime);
S.bgVid = S.bgVidItems[clipIndex].vid;
if (inXfade) {
  await seekVideoToTimeWeb(S.bgVidItems[(clipIndex + 1) % N].vid, 0 /* or matching offset */);
  S.bgVidNext = S.bgVidItems[(clipIndex + 1) % N].vid;
  S.bgVidFadeProgress = easeInOutCubic(xfadeAlpha);
} else {
  S.bgVidNext = null;
  S.bgVidFadeProgress = 0;
}
```

---

## 🎨 v1.2 — تَحسينات UX + تَجربة Undo/Redo (مُخطَّط)

**التاريخ:** 2026-07-12 — **مَلاحظات من المُستخدم أثناء اختبار v1.1.0**

الميزات الخَمس أُدرِجَت عبر TaskCreate (مَبدأ `feedback-user-suggestions-to-plan`).

### 1. 👁️ زَرّ إعماء/رؤية لكُلّ مَقطع خَلفيّة
- إضافة حَقل `S.bgVidItems[i].hidden = false` (افتراضياً)
- أَيقونة `👁️` / `👁️‍🗨️` بِجانب كُلّ item في القائمة
- تَعديل `switchToNextBgVid` + `updateBgVidCrossfade` + `activateBgVidByIndex` لتَخطّي المخفيّ
- **الفائدة:** إبقاء المَقطع في المَشروع دون استخدامه (تَجارب / تَفعيل مُؤقّت)

### 2. ✂️ تَحديد start/end لكُلّ مَقطع خَلفيّة (per-clip trim)
- حَقلا `S.bgVidItems[i].trimStart, trimEnd` (اختياريّان)
- UI: حقلا زَمَن أَو trim slider مُصغَّر بِجانب كُلّ item في القائمة
- تَعديل:
  - `getBgClipAtTime` (المُقترَح لِعَطَب #1 في v1.1.1) لِيَستَخدِمهما
  - Preview `updateBgVidCrossfade` — استبدال `cur.duration` بـ`trimEnd - trimStart`
  - Desktop ffmpeg: إضافة `-ss trimStart -to trimEnd` قبل كُلّ input في `filter_complex`
- الـtrim العالميّ `bg-vid-trim-on` الحاليّ يُصبح deprecated (أَو fallback)
- **الفائدة:** استخدام جُزء مُعيَّن من كُلّ فيديو (بَدَل مَقطعٍ كامِل)

### 3. ↩️ زَرّ استعادة مَقطع مَحذوف
- عند حَذف مَقطع من `bgVidItems`: حَفظه في stack `_deletedBgVidStack[]`
- زَرّ "استعادة آخر مَحذوف" يُعيده لِمَوقعه الأَصليّ
- **إمّا مُنفَصِل أَو جُزء من نِظام undo/redo العامّ (بَند #4)**

### 4. 🔄 نِظام Undo/Redo عامّ — Prototype في multi-bg
> **User quote:** "سنجربه على هذه الميزة لنعممه لا حقا على الأقسام المناسبة"

- Prototype في قسم مقاطع الخَلفيّة أَوّلاً
- History stack + action definitions:
  - `bg.add(item)` / `bg.remove(idx)` / `bg.reorder(from, to)`
  - `bg.hide(idx, state)` / `bg.trim(idx, start, end)`
  - `bg.crossfade(ms)`
- Undo/Redo كُلّ إجراء
- سَهم دائريّ `↩️` / `↪️` في UI
- **بَعد النَجاح** — تَعميم عَلى أَقسام مُناسِبة:
  - النَصّ الحرّ + توقيت الشرائح
  - العلامة المائيّة
  - المُصادر (bg audio/image/logo)
  - القوالب المُطبَّقة

### 5. 🔘 توگل تَفعيل النَصّ + العلامة المائيّة (دون حَذف)
- **قسم النُصوص:** checkbox "تَفعيل النَصّ" بِجانب حَقل النَصّ الحُرّ
- **قسم العلامة المائيّة:** checkbox "تَفعيل العلامة المائيّة" بِجانب حَقلها
- إذا مُعطَّل: النَصّ يَبقى في `S`/الـinput، لكن `drawVerse`/`drawWatermark` تَتَخطّاه
- نَمط مُماثِل لِـsilent-mode toggle
- **الفائدة:** تَجربة الريل بدون النَصّ / بدون العلامة، ثُمّ إعادة تَفعيلها فَوراً

### قَرارات مُعلَّقة
1. **الجُدولة:** هَل نُلحق هذه الخَمسة بـv1.1.1 (مَع إصلاحات multi-bg الأَربعة → v1.2.0)؟ أَم v1.1.1 يَبقى إصلاحاً بَحتاً وv1.2.0 يَحمِل الميزات الجَديدة؟
2. **Undo/Redo:** نَبدأ بِنَمط بَسيط (action stack خَطّيّ 20 خطوة)، أَم كامِل (redo + branching)؟
3. **العلاقة مَع #2:** per-clip trim قد يَحُلّ حاجة #1 (إعماء = trim(0,0))? أَم نَبقى مَع اثنَين مُنفَصِلَين لِلوُضوح؟

---

## 🔮 ما بعد v1.1.x

| الفكرة | الوصف |
|---|---|
| **🌟 تَوسيع وَحدة الحديث الشريف** | إدراج الكُتُب التِّسعة الكامِلة (~50,884 حَديثاً) — انظر القسم التَفصيليّ أَدناه |
| **مكتبة قوالب من المجتمع** | استيراد/تصدير JSON + معرض على فرع `templates/` |
| **مولّد بطاقات اقتباس** | إخراج صورة ثابتة (PNG) من نفس المحرّك |
| **قصص الأنبياء + السيرة النبويّة** | مقاطع قصيرة مع تواريخ |
| **بصمة المنشئ** | حقل اختياريّ للداعية: "بقلم: فلان" بـ badge |
| **محرّر طبقات (Layers)** | بنمط CapCut لمحتوى متقدّم |
| **GPU shaders للأداء** | تسريع تصدير 5× للويب |
| **نسخ macOS / Windows** | DMG و EXE من نفس قاعدة Electron |
| **TTS مَحلّيّ بـPiper** | تَوليد قِراءة بدون إنترنت (~50MB model) |
| **Workdir للهاتف** | Capacitor Filesystem → Documents/GT-SIRM/ |

---

## 📚 خُطّة تَوسيع وَحدة الحديث الشريف

### الحالة الحاليّة (v1.0.0)
- **90 حَديثاً** فقط: الأربعون النووية (40) + مُختارات من رياض الصالحين (50)
- مَصدر داخليّ: `hadith-data.js` (classic script يَضَع `window.HADITH_DATA`)

### المَصدر المُقترَح لِلتَوسعة
**[AhmedBaset/hadith-json](https://github.com/AhmedBaset/hadith-json)** — مُستودع مَفتوح يَحوي **50,884 حَديثاً** في **17 مَجموعة** بصيغة JSON.

#### المَجموعات المُتاحة
**الكُتُب التِّسعة:**
1. صَحيح البُخاريّ
2. صَحيح مُسلم
3. سُنَن أبي داود
4. جامِع التِّرمذيّ
5. سُنَن النَّسائيّ
6. سُنَن ابن ماجَه
7. مُوطَّأ مالك
8. مُسنَد أحمد *(فُصول 8-30 ناقصة في المَصدر)*
9. سُنَن الدَّارميّ

**مَجموعات إضافيّة:**
10. رياض الصالحين *(مَوجود جُزئيّاً في v1.0)*
11. الشَّمائل المُحَمَّديّة
12. بُلوغ المَرام
13. الأدب المُفرَد
14. مِشكاة المَصابيح
15. الأربعون النَّوويّة *(مَوجودة في v1.0)*
16. الأربعون القُدسيّة
17. أربعون الشاه ولي الله

### بِنية البيانات (Schema)
```json
{
  "id": 1,
  "chapterId": 1,
  "bookId": 1,
  "arabic": "...",
  "english": { "narrator": "...", "text": "..." }
}
```

### خُطّة التَنفيذ التَدريجيّ
- **v1.1 — الكُتُب السِتّة (الصِحاح + السُنَن الأربعة)** ≈ 35,000 حَديث
  - تَنزيل JSON من الـrelease tag (pin to specific version لتَجنّب breaking changes)
  - تَحويل لصيغة `HADITH_DATA` الحاليّة (chapters / hadiths)
  - **إضافة تَصحيح الإسناد** يَدويّاً أو من مَصدر مُكمِّل (الـjson لا يَحوي grading) — مَطلوب للإلتزام بقَواعد المَشروع
  - تَجزئة بحَسب الكِتاب (lazy-load) لتَجنّب bundle ضَخم
- **v1.2 — البَقيّة:** مُوطَّأ مالك + مُسنَد أحمد + سُنَن الدَّارميّ + المَجموعات الإضافيّة
- **بحث + تَصفية** بالكِتاب/الباب/الراوي/درجة التَصحيح

### اِعتبارات شَرعيّة وفِكريّة
- **شارة التَصحيح إلزاميّة** لكلّ حَديث (صَحيح/حَسَن/ضَعيف). إن لم تَتوفّر في المَصدر، إمّا يَدويّاً أو من مَصدر آخَر مَوثوق (مثل توثيقات الألبانيّ أو شُعَيب الأَرنؤوط).
- **التَّحقُّق من النَصّ العَربيّ** قبل الإدراج — لا اعتماد على scrapers بشَكل أعمى.
- **تَقسيم الـbundle** لتَجنّب تَحميل 50MB JSON على الهاتف.

### اعتبارات تِقنيّة
- المَلفّ الواحد للبخاريّ ~7,000 حَديث = ~5MB JSON. مَجموع الـ17 = ~35MB.
- **Lazy-load** عَبر `<script>` ديناميكيّ عند فَتح الوَحدة لكلّ كِتاب.
- **Service Worker** يُخزّن المَجموعة عند أوّل استخدام.
- **Pin to tag** لتَجنّب breaking changes (مَوصى به في المُستودع المَصدر).
- **Attribution** في تَبويب "حول": "بيانات الحَديث: AhmedBaset/hadith-json (مَصدرها sunnah.com)"

---

## 🔗 العلاقة مع GT-SQRM

GT-SIRM **يَرِث** كلّ ميزات GT-SQRM v3.0 — لا يُلغيها. النسختان تتعايشان:
- **GT-SQRM** = صانع ريلز مُكرَّس للقرآن — بسيط ومركَّز
- **GT-SIRM** = صانع ريلز إسلاميّ شامل — مرن وموسَّع
- **معالج "🔄 استيراد إعدادات GT-SQRM"** داخل GT-SIRM لنقل: القوالب · القرّاء · الخطوط

---

## 📝 ملاحظات للمساهمين

- اقرأ [`CLAUDE.md`](./CLAUDE.md) قبل المساهمة
- كلّ تغيير قابل للحمل يُطبَّق في النسختَين معاً
- الأحاديث: لا تُضاف بدون مصدر ودرجة تصحيح
- الأقوال: لا تُضاف بدون توثيق قائلها
- لا تُكسر مزامنة النسختَين — راجع جداول `diff` في `CLAUDE.md`
